query findSystemEnvPath {
  instances {
    entities(label: "/org/inexor/system/env/path") {
      id
      label
      type {
        namespace
        name
      }
      properties(names: [
        "label"
        "name"
        "value"
      ]) {
        name
        value
      }
    }
  }
}
