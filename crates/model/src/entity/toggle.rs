use crate::model::entity_type;
use crate::NAMESPACE_LOGICAL;

// All properties are defined in the component(s)

entity_type!(ENTITY_TYPE_TOGGLE, NAMESPACE_LOGICAL, ENTITY_TYPE_NAME_TOGGLE, "toggle");
