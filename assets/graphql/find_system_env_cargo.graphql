query findSystemEnvCargo {
  instances {
    entities(label: "/org/inexor/system/env/cargo") {
      id
      label
      type {
        namespace
        name
      }
      properties(names: [
        "label"
        "name"
        "value"
      ]) {
        name
        value
      }
    }
  }
}
