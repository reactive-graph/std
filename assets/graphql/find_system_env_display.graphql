query findSystemEnvDisplay {
  instances {
    entities(label: "/org/inexor/system/env/display") {
      id
      label
      type {
        namespace
        name
      }
      properties(names: [
        "label"
        "name"
        "value"
      ]) {
        name
        value
      }
    }
  }
}
