query getAllConfigs {
  instances {
    entities(type: "config_file") {
      id
      label
      description
      properties(
        names: [
          "trigger",
          "filename",
          "result"
        ]
      ) {
        name
        value
      }
    }
  }
}
