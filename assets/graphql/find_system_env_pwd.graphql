query findSystemEnvPwd {
  instances {
    entities(label: "/org/inexor/system/env/pwd") {
      id
      label
      type {
        namespace
        name
      }
      properties(names: [
        "label"
        "name"
        "value"
      ]) {
        name
        value
      }
    }
  }
}
