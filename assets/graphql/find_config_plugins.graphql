query findSystemEnvCargo {
  instances {
    entities(label: "/org/inexor/system/config/plugins") {
      id
      type {
        name
      }
      properties(names: [
        "label"
        "filename"
        "configuration"
      ]) {
        name
        value
      }
    }
  }
}
